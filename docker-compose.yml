services:
  auditor:
    build: .
    container_name: health-auditor

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./metrics.prom:/app/metrics.prom